# status: floor — 1 row
#
# English on PDI's desktop console. Checked by
# test_the_desktop_and_the_phone_say_different_things.py, and ratcheted in both
# directions: the count may not rise, and a fall of more than sixty is treated
# as an extractor that stopped matching rather than a round of work.
#
# ## What this is
#
# The sibling products each audited their console's language rounds ago and
# drove the number to a floor. **Nothing had ever counted this one.** Until
# 0.48.1 there was no `app/src/l10n.ts` in this repo, no record, and no guard:
# fourteen screens, 250 strings, all English, while all three native shells
# carry a ten-language table and the backend answers a tenant in the language
# they chose.
#
#     0.48.2   250 → 229   Guiding — the language picker itself
#     0.48.3   229 → 177   Custody and Continuity
#     0.60.4   177 → 233   **the reader, not the console** — see below
#     0.60.4   233 → 225   VersionGuard, which the old reader saw none of
#     0.60.5   225 → 154   Carriers and Exchange
#     0.60.6   154 → 168   **the reader again** — see below
#     0.60.6   168 →  91   Positions and Bridges
#      (next)    91 →  76   Continuity and Custody — *finished* this time
#      (next)    76 →  32   Records, Problems, Settings, Tenants, ProblemNotice
#      (next)    32 →   0   Keys, Overview, Audit, Operations, VaultLight, App
#
# ## The one rise in this record, and why it is not a regression
#
# Until 0.60.4 this file was measured by three regexes, the first of which was
# `>\s*([A-Z][^<>{}\n]{2,})\s*<`. QRME and JIM both abandoned that reader
# rounds ago — three separate regexes over the same source each hid real
# strings — and moved to `app/scripts/jsx-text.mjs`, which parses the file
# with TypeScript's own parser and returns every `JsxText` node. This product
# kept the regex, and nothing had compared the two.
#
# The three characters that pattern forbids are the three shapes most of this
# console's prose takes:
#
#   * `\n` — any sentence long enough to wrap. The paragraph in
#     `ProblemNotice` explaining what a problem report does and does not carry
#     is four source lines: one string to a reader, no string at all to this.
#   * `{}` — any sentence with a value in the middle. `VersionGuard` reads
#     *This app is v{CONSOLE_VERSION}, but the backend at {getBase()} is
#     v{backend} — an older install is still running*. Three interpolations
#     cut that into fragments and the pattern rejected every one. The screen
#     whose whole job is to explain why an install answers "Not Found" was the
#     least visible thing in the console.
#   * a leading capital — *no tenant selected*, *entries verified*,
#     *what reaches into this vault*, *one per integrating system*.
#
# Fifty-six strings, a quarter of the true total, and hidden in the direction
# that makes a ratchet look satisfied. Both localization rounds above were
# graded against a number that was low.
#
#     asked     how much English does this pattern match
#     mattered  how much English does a person read
#
# The rise from 177 to 233 is the reader getting honest in one step. The fall
# to 225 is `VersionGuard`, wired the same day, because a finding that only
# raises a number is half a finding.
#
# ## The order, and why it is not the order of the counts
#
# **Guiding** went first because it holds the language picker: the screen where
# a person chooses their language is the first that has to read in it.
#
# **Custody** and **Continuity** went next, ahead of larger screens, because
# they are decisions rather than descriptions — the same criterion this audit
# has used since the alarm surface. Custody answers *can the operator decrypt
# this*, and carries the sentence naming the honest measure of bring-your-own-
# key: how much of the vault the operator could not touch even when asked to.
# Continuity is what happens to a sealed file after a death — a standing
# instruction that grants nothing when written, an activation that needs a
# different credential from the one that wrote it, and an heir who holds two
# separate secrets of which neither works alone.
#
# ## Localizing a screen blinds the guards that grep it
#
# Wiring Guiding.tsx turned `test_the_guide_screen_keeps_both_of_its_refusals`
# red at 0.48.2, and it was right to. That check makes sure the console keeps
# saying the two things the server insists on, and it did it by grepping the
# screen for the English. The sentence moved into `l10n.ts`; the screen still
# says it; the grep went blind.
#
#     asked     is this sentence in the screen file
#     mattered  does the screen say it, in every language it offers
#
# **0.48.3 hit four more** — the custody question, the reseal note, the
# difference between revoking a grant and revoking a bequest, and the
# difference between *nothing paged* and *nothing could have been paged*. All
# four now go through `_says()` in `test_the_door_and_the_wire.py`: the screen
# must ask for the key and the table must hold it in all ten languages, which
# is stricter than the grep it replaces. Six of the fourteen screen-greps in
# that file have now been converted; the remaining eight go blind the round
# their screen is localized, and are expected to.
#
# ## What is left
#
# Worst first, by the count in each file:
#
# Nothing. The list is empty, which is why the first line of this file now
# reads **floor** rather than backlog: there is no backlog left to work down,
# and the only direction the number can move is up. It may not.
#
# The prose still visible on Continuity that no key covers is server-rendered —
# it arrives already in the reader's language, or does not, at the backend's
# discretion rather than the console's.
#
# ceiling: 0
#
# ## 0.60.5, and the criterion for what went next
#
# Four screens held 141 of the 225: Carriers 38, Positions 36, Bridges 34,
# Exchange 33. Carriers and Exchange went first on the criterion this record
# has used since the alarm surface — **decisions before descriptions**.
#
# **Carriers** is the console side of the surface whose *landing pages* were
# the finding in `test_the_strangers_language.py`. A courier holding a crate
# scans a code and reads the card in their own language; the holder who places
# the code, reads the chain of custody and answers a ring read English. One
# half of a two-sided surface had been taught to speak and the other had not.
#
# **Exchange** is where nothing has an undo. A receive token served exactly
# once, a seal that opens into the audit chain rather than being looked at, an
# intake that is a one-way door somebody else walks through. A person reading
# a language this console does not speak was making those decisions from the
# button labels alone.
#
# Two things fell out of doing it:
#
#   * The TypeScript compiler caught `transfers.map((t) => …)` shadowing the
#     translator the moment `t` became a function. A reading guard would not
#     have; the type checker did, on the first run.
#   * `test_the_desktop_and_the_phone_say_the_same_thing` caught seven rows
#     where the new console wording and a shell's wording for the same English
#     disagreed — *Recipient*, *Filename*, *Refresh*. The shells' wordings win:
#     two tables saying one English in two Spanishes is one product making two
#     claims to one reader.

#
# ## 0.60.6 — the second rise, and what a word boundary is a claim about
#
# The reader that got honest at 0.60.4 was still asking one question wrong. It
# counted a string as English if it matched `[A-Za-z]\s[A-Za-z]` — a letter, a
# space, a letter — or was a single capitalised word. Four headings on the very
# screen this round localizes have neither:
#
#     Role &amp; industry
#     Decision-making &amp; oversight
#     Bottlenecks &amp; obsolescence
#     Human-in-the-loop
#
# Every space in the first three sits beside an HTML entity, so the character
# before it is `;` and the character after it is `&`. The fourth has no space at
# all. All four are English a person reads off the screen, and none of them was
# ever counted. So were `Re-verify`, `publish — out`, `hard — gone`, `soft —
# recoverable`, `Keys &amp; Retention` (twice) and `append-only · SHA-256
# hash-chained`.
#
#     asked     is there a space with a letter on both sides
#     mattered  does this read as more than one word
#
# Fourteen strings, and this is the third time in this arc that the measurement
# was the bug: `>\s*([A-Z][^<>{}\n]{2,})\s*<` at 0.60.4, three separate
# regexes over one source before that, and now a word boundary that only knows
# about spaces. Each time the fix raised the number before any work lowered it,
# which is the only order that is honest.
#
# `_PHRASE` decodes entities first — the reader sees the `&` the browser draws,
# not the `&amp;` the source stores — and then looks for two runs of letters
# joined by anything that is not `/`, `_` or `.`. Those three are excluded
# deliberately, and each excludes a real string in this console: a path
# (`POST /profiles/{id}/chat → 500`), an identifier (`PDI_ADMIN_TOKEN`) and a
# filename (`report.pdf`). `test_the_reader_reads_more_than_a_space` holds both
# halves — the four headings must count, the three non-sentences must not — so
# a later simplification of the pattern cannot quietly re-blind it.
#
# ## What went with it, and why these two
#
# Positions and Bridges were the two remaining screens of the four that held 141
# strings between them, and they are opposite kinds of screen.
#
# **Positions** is the longest form in the product. It asks an operator to
# describe their own working life — what they manage, what they are accountable
# for, which of their tasks have gone stale — and hands back an assistant
# blueprint built from the answers. A misread question there does not produce a
# confusing screen; it produces a blueprint for a role nobody holds. Its option
# lists are translated too, though no reader counts them: a heading in Spanish
# above eight English chips is a half-answer, and the chip *values* stay English
# keys on the wire, which is the whole reason the words can move.
#
# **Bridges** is the opposite — mostly prose, and the prose is a promise. Two
# paragraphs say what the vault will *not* do: a contribution listing is a count
# and a set of keys and never contents, and everything a bound robot sends in is
# sealed under the tenant's key like anything else. A promise about custody only
# counts if the person it is made to can read it.
#
# One row was decided rather than translated. `bri.source.ph` is the placeholder
# `jim-mini` — a product's own name, the same word in all ten languages. It has
# a row so that the record shows a decision instead of an oversight.
#
# ## Wired is not finished, and this list could not tell them apart
#
# Every round above picked its next screen off the list under *What is left*,
# worst count first. That ordering carries an assumption nothing had ever
# checked: that a screen which has been *through* a localization round is done
# with. The list is built from screens that still hold English, so a screen
# that holds any less than all of it drops down the list and eventually off
# the bottom of attention — not because it was finished, because it was
# cheaper than the next one.
#
#     asked     does this screen import the translator
#     mattered  does this screen still hold English
#
# Eight of this console's files import `t` from `../l10n`. Six hold no
# English. Two had held some since **0.48.3**, the round that claimed them:
# `Continuity.tsx` with ten strings and `Custody.tsx` with five. Twelve
# releases, five localization rounds, and both sat on the finished side of the
# ledger the whole time while the audit worked down a list neither was on.
#
# The sharp part is not that fifteen strings were missed. It is what six of
# Continuity's ten turned out to be:
#
#     Name                                  co.name.ph
#     Role                                  co.role.ph
#     Read                                  co.read
#     Set                                   co.set
#     What can I open?                      co.whatopen
#     Nothing is readable under that grant. co.nothing.readable
#
# Six rows this table **already held**, in all ten languages, on the screen
# that imports the translator to reach five of its neighbours. The screen had
# the key and typed the word anyway. Not a missing translation — a translation
# already written, already paid for, and never asked for. Only four rows
# needed writing (`co.attested`, `co.attested.dated`, `co.revoke`, `co.retry`)
# and Custody's five, of which `cu.you` is decided rather than translated:
# the English *You are responsible for:* is second person, which in German,
# Spanish and Italian forces the T–V question this table avoids everywhere
# else, so the nine translations name the party instead of the pronoun.
#
# ## The guard, and what it can and cannot see
#
# `test_a_screen_that_imports_the_translator_holds_no_english` holds the claim
# from here on: **a screen that asks the table for a word may not also
# hard-code one.** A file is free to be untranslated — that is what the
# ceiling above is for, and it may take as many rounds as it takes. What it
# may not be is half-translated and counted as whole. It failed on the round
# it was written, naming both files and all fifteen strings, which is how it
# was known to work before either was fixed.
#
# It reads what `_console_english` reads: JSX text, `placeholder` and `title`.
# So it sees what a person reads off the screen and **not** English inside a
# `setSaid(…)` or a template literal — `Handed back to deployment custody.` in
# Custody is still English and still uncounted, here and in every screen this
# record has ever called done. That is the next reader question rather than
# this one, and it is written down here so the guard is not read as claiming
# more than it checks.
#
# ## The five that went with them, and the two kinds of string
#
# Records, Problems, Settings, Tenants and ProblemNotice — 44 strings, 76 → 32.
# Records is the vault's own screen and went first on the criterion this record
# has used since the alarm surface: sealing and opening a record is the thing
# the product is *for*, and it read in English only. Problems and ProblemNotice
# are one feature in two places — what leaves the device, and the consent asked
# before anything does — and it would be a strange product that asks for that
# consent in a language the asking is not offered in.
#
# Seven of the 44 were English the tables already held. Four are the shells' —
# `nrec.t.records`, `tab.vault`, `nrec.key`, `nrec.seal` — and the wordings are
# copied from them rather than written again, which is the split
# `test_the_desktop_and_the_phone_say_the_same_thing` exists to catch, avoided
# in advance for once instead of after it went red. Three more were already in
# this table under `pos.` and are called by their existing keys.
#
# ## What the ratchet cannot see, and what was done about it anyway
#
# The reader counts JSX text, `placeholder` and `title`. It does not count a
# string literal inside a JSX *expression*, which is where a surprising amount
# of this console's English lives: `{busy ? "Creating…" : "Create tenant"}` in
# Tenants, the whole offline-posture block in Settings, six outcome sentences
# in a module-level map in Problems.
#
#     asked     what does the ratchet count
#     mattered  what does a person read
#
# Those were translated too, and they are the reason the row count added in
# this round is far above 44. A button reading *Create tenant* under a Spanish
# heading is English to the person looking at it whatever the reader thinks.
# `Problems.tsx`'s `OUTCOME` map needed a shape change to allow it: a
# module-level constant cannot ask for a language, so it now holds keys and the
# screen resolves them.
#
# One shadowing, caught by `npx tsc --noEmit` on the first run and not by
# reading: `const t = await api.createTenant(…)` in Tenants, where `t` became
# the translator the moment the screen was wired. That is the second time in
# three rounds the type checker has found this exact shape — 0.60.5 had
# `transfers.map((t) => …)` in Exchange — which is why the method says to drive
# it rather than read it.

console: 0

# ## 0 — the last six, and what a floor of zero is a claim about
#
# Keys, Overview, Audit, Operations, VaultLight and App. Thirty-two strings,
# and five of them were English a table already held: `cu.versions` for the key
# list, and the shells' `tab.overview` and `tab.audit`, whose wordings are
# copied rather than written again for the third round running.
#
# `app.sub` was decided rather than assumed. *Private Data Infrastructure* is
# what `PDI` stands for, and a reader who cannot read English gets nothing from
# it — so the description moves and the acronym does not, the same split every
# product in this estate makes between a name and a sentence.
#
# `VaultLight` is the screen this round nearly left half-done, and it is worth
# recording why. Its English lives entirely in `aria-label` and `title`, which
# is to say it is invisible to everybody except the reader who most depends on
# it. Three of its four strings went in the first pass and the fourth — the
# `aria-label` on the status container itself — was found by re-measuring
# rather than by reading the file again.
#
#     asked     does this screen look translated
#     mattered  does the screen reader read it in the reader's language
#
# A floor of zero is a stronger claim than a backlog of zero would be. A
# backlog says *nothing left to do*; a floor says *nothing may be added*. Every
# new screen from here arrives with its strings in the table or it arrives red,
# and `test_a_screen_that_imports_the_translator_holds_no_english` holds the
# other half: a screen may not be wired and unfinished at the same time.
