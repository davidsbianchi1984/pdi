# status: backlog — 1 row
#
# English on PDI's desktop console. Checked by
# test_the_desktop_and_the_phone_say_different_things.py, and ratcheted in both
# directions: the count may not rise, and a fall of more than sixty is treated
# as an extractor that stopped matching rather than a round of work.
#
# ## What this is
#
# The sibling products each audited their console's language rounds ago and
# drove the number to a floor. **Nothing had ever counted this one.** Until
# 0.48.1 there was no `app/src/l10n.ts` in this repo, no record, and no guard:
# fourteen screens, 250 strings, all English, while all three native shells
# carry a ten-language table and the backend answers a tenant in the language
# they chose.
#
#     0.48.2   250 → 229   Guiding — the language picker itself
#     0.48.3   229 → 177   Custody and Continuity
#     0.60.4   177 → 233   **the reader, not the console** — see below
#     0.60.4   233 → 225   VersionGuard, which the old reader saw none of
#     0.60.5   225 → 154   Carriers and Exchange
#
# ## The one rise in this record, and why it is not a regression
#
# Until 0.60.4 this file was measured by three regexes, the first of which was
# `>\s*([A-Z][^<>{}\n]{2,})\s*<`. QRME and JIM both abandoned that reader
# rounds ago — three separate regexes over the same source each hid real
# strings — and moved to `app/scripts/jsx-text.mjs`, which parses the file
# with TypeScript's own parser and returns every `JsxText` node. This product
# kept the regex, and nothing had compared the two.
#
# The three characters that pattern forbids are the three shapes most of this
# console's prose takes:
#
#   * `\n` — any sentence long enough to wrap. The paragraph in
#     `ProblemNotice` explaining what a problem report does and does not carry
#     is four source lines: one string to a reader, no string at all to this.
#   * `{}` — any sentence with a value in the middle. `VersionGuard` reads
#     *This app is v{CONSOLE_VERSION}, but the backend at {getBase()} is
#     v{backend} — an older install is still running*. Three interpolations
#     cut that into fragments and the pattern rejected every one. The screen
#     whose whole job is to explain why an install answers "Not Found" was the
#     least visible thing in the console.
#   * a leading capital — *no tenant selected*, *entries verified*,
#     *what reaches into this vault*, *one per integrating system*.
#
# Fifty-six strings, a quarter of the true total, and hidden in the direction
# that makes a ratchet look satisfied. Both localization rounds above were
# graded against a number that was low.
#
#     asked     how much English does this pattern match
#     mattered  how much English does a person read
#
# The rise from 177 to 233 is the reader getting honest in one step. The fall
# to 225 is `VersionGuard`, wired the same day, because a finding that only
# raises a number is half a finding.
#
# ## The order, and why it is not the order of the counts
#
# **Guiding** went first because it holds the language picker: the screen where
# a person chooses their language is the first that has to read in it.
#
# **Custody** and **Continuity** went next, ahead of larger screens, because
# they are decisions rather than descriptions — the same criterion this audit
# has used since the alarm surface. Custody answers *can the operator decrypt
# this*, and carries the sentence naming the honest measure of bring-your-own-
# key: how much of the vault the operator could not touch even when asked to.
# Continuity is what happens to a sealed file after a death — a standing
# instruction that grants nothing when written, an activation that needs a
# different credential from the one that wrote it, and an heir who holds two
# separate secrets of which neither works alone.
#
# ## Localizing a screen blinds the guards that grep it
#
# Wiring Guiding.tsx turned `test_the_guide_screen_keeps_both_of_its_refusals`
# red at 0.48.2, and it was right to. That check makes sure the console keeps
# saying the two things the server insists on, and it did it by grepping the
# screen for the English. The sentence moved into `l10n.ts`; the screen still
# says it; the grep went blind.
#
#     asked     is this sentence in the screen file
#     mattered  does the screen say it, in every language it offers
#
# **0.48.3 hit four more** — the custody question, the reseal note, the
# difference between revoking a grant and revoking a bequest, and the
# difference between *nothing paged* and *nothing could have been paged*. All
# four now go through `_says()` in `test_the_door_and_the_wire.py`: the screen
# must ask for the key and the table must hold it in all ten languages, which
# is stricter than the grep it replaces. Six of the fourteen screen-greps in
# that file have now been converted; the remaining eight go blind the round
# their screen is localized, and are expected to.
#
# ## What is left
#
# Worst first, by the count in each file:
#
#     Positions.tsx    35        Settings.tsx      7
#     Bridges.tsx      29        Tenants.tsx       7
#     Exchange.tsx     27        Overview.tsx      5
#     Carriers.tsx     24        the rest         23
#     Records.tsx      11
#     Continuity.tsx    9        (its remaining rows are server-rendered)
#
# ceiling: 154
#
# ## 0.60.5, and the criterion for what went next
#
# Four screens held 141 of the 225: Carriers 38, Positions 36, Bridges 34,
# Exchange 33. Carriers and Exchange went first on the criterion this record
# has used since the alarm surface — **decisions before descriptions**.
#
# **Carriers** is the console side of the surface whose *landing pages* were
# the finding in `test_the_strangers_language.py`. A courier holding a crate
# scans a code and reads the card in their own language; the holder who places
# the code, reads the chain of custody and answers a ring read English. One
# half of a two-sided surface had been taught to speak and the other had not.
#
# **Exchange** is where nothing has an undo. A receive token served exactly
# once, a seal that opens into the audit chain rather than being looked at, an
# intake that is a one-way door somebody else walks through. A person reading
# a language this console does not speak was making those decisions from the
# button labels alone.
#
# Two things fell out of doing it:
#
#   * The TypeScript compiler caught `transfers.map((t) => …)` shadowing the
#     translator the moment `t` became a function. A reading guard would not
#     have; the type checker did, on the first run.
#   * `test_the_desktop_and_the_phone_say_the_same_thing` caught seven rows
#     where the new console wording and a shell's wording for the same English
#     disagreed — *Recipient*, *Filename*, *Refresh*. The shells' wordings win:
#     two tables saying one English in two Spanishes is one product making two
#     claims to one reader.

console: 154
