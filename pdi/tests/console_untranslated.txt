# status: backlog — 1 row
#
# English on PDI's desktop console. Checked by
# test_the_desktop_and_the_phone_say_different_things.py, and ratcheted in both
# directions: the count may not rise, and a fall of more than sixty is treated
# as an extractor that stopped matching rather than a round of work.
#
# ## What this is
#
# The sibling products each audited their console's language rounds ago and
# drove the number to a floor. **Nothing had ever counted this one.** Until
# 0.48.1 there was no `app/src/l10n.ts` in this repo, no record, and no guard:
# fourteen screens, 250 strings, all English, while all three native shells
# carry a ten-language table and the backend answers a tenant in the language
# they chose.
#
#     0.48.2   250 → 229   Guiding — the language picker itself
#     0.48.3   229 → 177   Custody and Continuity
#
# ## The order, and why it is not the order of the counts
#
# **Guiding** went first because it holds the language picker: the screen where
# a person chooses their language is the first that has to read in it.
#
# **Custody** and **Continuity** went next, ahead of larger screens, because
# they are decisions rather than descriptions — the same criterion this audit
# has used since the alarm surface. Custody answers *can the operator decrypt
# this*, and carries the sentence naming the honest measure of bring-your-own-
# key: how much of the vault the operator could not touch even when asked to.
# Continuity is what happens to a sealed file after a death — a standing
# instruction that grants nothing when written, an activation that needs a
# different credential from the one that wrote it, and an heir who holds two
# separate secrets of which neither works alone.
#
# ## Localizing a screen blinds the guards that grep it
#
# Wiring Guiding.tsx turned `test_the_guide_screen_keeps_both_of_its_refusals`
# red at 0.48.2, and it was right to. That check makes sure the console keeps
# saying the two things the server insists on, and it did it by grepping the
# screen for the English. The sentence moved into `l10n.ts`; the screen still
# says it; the grep went blind.
#
#     asked     is this sentence in the screen file
#     mattered  does the screen say it, in every language it offers
#
# **0.48.3 hit four more** — the custody question, the reseal note, the
# difference between revoking a grant and revoking a bequest, and the
# difference between *nothing paged* and *nothing could have been paged*. All
# four now go through `_says()` in `test_the_door_and_the_wire.py`: the screen
# must ask for the key and the table must hold it in all ten languages, which
# is stricter than the grep it replaces. Six of the fourteen screen-greps in
# that file have now been converted; the remaining eight go blind the round
# their screen is localized, and are expected to.
#
# ## What is left
#
# Worst first, by the count in each file:
#
#     Positions.tsx    35        Settings.tsx      7
#     Bridges.tsx      29        Tenants.tsx       7
#     Exchange.tsx     27        Overview.tsx      5
#     Carriers.tsx     24        the rest         23
#     Records.tsx      11
#     Continuity.tsx    9        (its remaining rows are server-rendered)
#
# ceiling: 177
console: 177
