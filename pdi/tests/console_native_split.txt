# status: floor — 0 rows
#
# English strings held by both the desktop console's table and a shell's, on
# which no console row and no native row share a wording. Checked by
# test_the_desktop_and_the_phone_say_different_things.py, matched exactly in
# both directions.
#
# ## Three zeros, three different reasons
#
# **0.48.1.** Empty because `app/src/l10n.ts` did not exist. The zero meant
# *nothing to compare*, and the file said so, with a check that fired if that
# phrase were removed while the table was still absent.
#
# **0.48.2.** The table arrived with 36 rows and one English string in common
# with each shell. Still empty, now because the two tables were about different
# screens. This file predicted what would happen next:
#
#     it becomes a real record the moment a screen exists on both sides
#
# **0.48.3.** It did, within one round. Custody and Continuity brought the
# table to 133 rows, and the guard immediately found one: `co.admin.ph`
# against Android's `nadm.token`, both **Admin token**, disagreeing in
# Portuguese, Hindi and Arabic. The console adopted the shells' wording — the
# rule 0.48.1 settled — and the count returned to zero.
#
#     asked     do the console and the shells disagree
#     mattered  is anything watching while they grow
#
# So this is a floor that has now done its job once, at a scale of one row,
# which is the whole argument for building the table with the comparison
# already running. The sibling products reached 102 and 25 disagreements by
# growing two tables past each other for many releases with nothing watching.
#
# ceiling: 0
