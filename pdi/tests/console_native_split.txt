# status: floor — 0 rows
#
# English strings held by both the desktop console's table and a shell's, on
# which no console row and no native row share a wording. Checked by
# test_the_desktop_and_the_phone_say_different_things.py, matched exactly in
# both directions.
#
# ## The table exists now, and the zero means something different
#
# When this record was written at 0.48.1 it was empty because `app/src/l10n.ts`
# **did not exist**: this console had no localization table at all, and the
# zero meant *nothing to compare* rather than *nothing wrong*. The file said so
# in its own text, and a check fired if that phrase were ever removed while the
# table was still absent.
#
# 0.48.2 created the table and wired the first screen. So the zero now means
# something else, and this file has to say which — the whole reason
# `test_a_record_that_outlived_the_code.py` exists.
#
#     asked     do the console and the shells disagree
#     mattered  do they yet have anything to disagree about
#
# **36 console rows, and one English string in common with each shell.** The
# console's rows are the Guiding screen's (`gd.*`); the shells' are the file,
# recipient and transfer screens' (`nfil.*`, `ntr.*`). Two tables about
# different screens cannot contradict each other yet.
#
# So this is a floor, but a shallow one: it becomes a real record the moment a
# screen exists on both sides, which `console_untranslated.txt` puts at 229
# strings and six screens away. The sibling products reached 102 and 25
# disagreements by growing two tables past each other without ever comparing
# them. This one is being grown with the comparison already running.
#
# ceiling: 0
